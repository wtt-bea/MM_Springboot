server:
  port: 80
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mm?useUnicode=true&characterEncoding=utf-8&&useSSL=false
    username: root
    password: 123456
  jackson:
    time-zone: GMT+8
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.mm_springboot.model